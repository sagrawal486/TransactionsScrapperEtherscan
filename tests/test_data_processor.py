from app.data_processing.ct_etherscan_processor import CTEtherscanProcessor

#Check if we are able to convert raw data into defined pydantic model
def test_normal_transactions_processor_change_to_model():
    assert 1 == 1


#Similarly do for other types of transactions
